[package]
name = "yamldump"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Dump and inspect YAML files from the terminal"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
serde_yaml = "0.9"
